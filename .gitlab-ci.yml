image: pluppen/auto-selenium-python:latest

stages:
  - test
  - deploy

pages:
  stage: deploy
  script:
    - echo "helo"
  artifacts:
    paths:
      - public
  only:
    changes:
      - "public/**/*.css"
      - "public/**/*.html"

html_validator:
  stage: test
  script:
    - ./tests/validate/html_validator.sh public
  only:
    changes:
      - "public/**/*.html"

css_validator:
  stage: test
  script:
    - ./tests/validate/css_validator.sh public
  only:
    changes:
      - "public/**/*.css"

e2e:website-test:
  stage: test
  services:
    - name: selenium/standalone-firefox
      alias: selenium_firefox
  before_script:
    - pip3 install selenium==4.0.0a6.post2 requests
  script:
    - php -S 0.0.0.0:8080 -t public public/index.html & 
    - python3 ./tests/webtest/website_tests.py
  only:
    changes:
      - "public/**/*.css"
      - "public/**/*.html"
