image: pluppen/auto-selenium-python:latest

stages:
  - test
  - deploy

pages:
  stage: deploy
  script:
    - lessc less/main.less public/css/style.css
  artifacts:
    paths:
      - public
  only:
    changes:
      - "public/**/*"
      - "less/**/*"

static_validation:
  stage: test
  script:
    - lessc less/main.less public/css/style.css
    - ./tests/validate/css_validator.sh public
    - ./tests/validate/html_validator.sh public
  only:
    changes:
      - "public/**/*"
      - "less/**/*"
      - "tests/validate/*"


e2e:website-test:
  stage: test
  services:
    - name: selenium/standalone-firefox
      alias: selenium_firefox
  script:
    - lessc less/main.less public/css/style.css
    - python3 -m http.server 8080 -d public > /dev/null 2>&1 &
    - cd tests/webtest
    - python3 -m unittest
  only:
    changes:
      - "public/**/*"
      - "less/**/*"
      - "tests/webtest/*"
